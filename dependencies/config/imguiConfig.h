#ifndef IMGUI_CONFIG_H
#define IMGUI_CONFIG_H

// Increase the 64k vertex limit by switching to 32bit indices
// This is required to render a lot of cell values as text when many cells are visible
#define ImDrawIdx unsigned int

#endif